import{addDays,dateKey,getDaySummary}from'./storage';
export const CHALLENGES=[{id:'reset7',name:'7-Day Hydration Reset',emoji:'🌱',days:7,targetDays:7,description:'Meet your personal goal for seven consecutive days.',access:'free'},{id:'workweek',name:'Perfect Workweek',emoji:'💼',days:7,targetDays:5,description:'Meet your goal on five weekdays.',access:'premium'},{id:'early',name:'Early Hydrator',emoji:'🌅',days:7,targetDays:5,description:'Log at least 500ml before 10am on five days.',access:'premium'},{id:'summer',name:'30-Day Consistency',emoji:'☀️',days:30,targetDays:24,description:'Meet your goal on 24 of 30 days.',access:'premium'},{id:'before8',name:'Done Before Eight',emoji:'🕗',days:14,targetDays:10,description:'Complete your goal before 8pm on ten days.',access:'premium'}];
export async function calculateChallengeProgress(c){const today=dateKey();let done=0;for(let i=0;i<c.days;i+=1){const key=addDays(today,-i),s=await getDaySummary(key);if(c.id==='early'){if(s.entries.filter(e=>new Date(e.timestamp).getHours()<10).reduce((a,e)=>a+e.amountMl,0)>=500)done+=1}else if(c.id==='before8'){if(s.entries.filter(e=>new Date(e.timestamp).getHours()<20).reduce((a,e)=>a+e.amountMl,0)>=s.goalMl)done+=1}else if(c.id==='workweek'){const w=new Date(`${key}T12:00:00`).getDay();if(w>=1&&w<=5&&s.metGoal)done+=1}else if(s.metGoal)done+=1}return{completedDays:done,targetDays:c.targetDays,percent:Math.min(100,Math.round(done/c.targetDays*100)),completed:done>=c.targetDays}}
